# Server port (use PORT on cloud platforms)
PORT=3847

# Artificial network delay in ms (0 = instant)
MOCK_DELAY_MS=300

# Path to mock JSON directory (default: ../assets/mock)
# MOCK_DATA_DIR=../assets/mock

# API prefix mounted on Express
API_PREFIX=/api/v1
