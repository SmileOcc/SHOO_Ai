```dart
1. 衍生 Provider（hos_flash_sale_controller.dart）
flashSaleMergedProductsProvider：仅在 pageData.products 或关注列表变化时重算
flashSaleMergedCouponsProvider：优惠券领取状态合并
mergeFlashSaleProducts：用 Set 做 O(1) 关注匹配，仅在 isFollowed 变化时 copyWith
删除 build 内调用的 mergedProducts() 及 state 上的 mergedCoupons getter

2. 页面拆分 + select 细粒度监听（hos_flash_sale_page.dart）
组件	优化
主 build
只 watch isInitialLoading、hasMore
_DayTabs / _SessionBar / _SortBar
各自 select 所需字段，返回 Sliver
_FlashSaleProductListSliver
独立 watch flashSaleMergedProductsProvider
_PromoEntries
点击时 ref.read(sessionProvider)，不再 watch
_CouponSection
使用 flashSaleMergedCouponsProvider

3. 列表优化
商品卡片加 ValueKey('sessionId:id')
外包 RepaintBoundary，并设 addRepaintBoundaries: false 避免重复
效果
切换排序 / 日期：头部与列表分区重建，不再整页 + 全量 mergedProducts() 重算
关注 / 取消关注：主要刷新商品列表 Provider，头部不再跟着 rebuild
分页加载：有 stable key，列表项更易复用

ref.read(sessionProvider) 在点击回调中比 ref.watch 更好 ，原因是：

1. 性能 ：避免 session 变化时触发列表项的重建
2. 语义 ：明确表达"一次性读取"的意图
3. 正确性 ：点击操作只需要当前时刻的 session 值，不需要响应后续变化

可以监听对应的属性变化，而不是整个状态树的变化
final pageData = ref.watch(
      flashSaleControllerProvider(activityId).select((s) => s.pageData),
    );

```dart
import 'package:flutter/material.dart';


class ListenerSimpleExample extends StatefulWidget {
  @override
  _ListenerSimpleExampleState createState() => _ListenerSimpleExampleState();
}

class _ListenerSimpleExampleState extends State<ListenerSimpleExample> with AutomaticKeepAliveClientMixin {

  @override
  bool get wantKeepAlive => true;

  @override
  Widget build(BuildContext context) {
    super.build(context);
    return Scaffold(
      appBar: AppBar(
        title: Text("Listener"),
      ),
      body: Center(
        child: Stack(
          children: [
            Listener(
              child: ConstrainedBox(
                  constraints: BoxConstraints.tight(Size(400, 200)),
                  child: Container(
                    color: Colors.greenAccent,
                  )
              ),
              onPointerDown: (event) => print("绿色盒子被点击了"),
            ),
            Listener(
              child: ConstrainedBox(
                constraints: BoxConstraints.tight(Size(400, 200)),
                child: Center(child: Text("点击文字", style: TextStyle(
                  color: Colors.white,
                  fontSize: 30
                ),)),
              ),
              onPointerDown: (event) => print("文字点击事件回调"),
              behavior: HitTestBehavior.deferToChild,
              // behavior: HitTestBehavior.opaque,
              // behavior: HitTestBehavior.translucent,
            )
          ],
        ),
      ),
    );
  }
}

当属性设置为HitTestBehavior.deferToChild控制台输出结果

我们这里演示每次都是先点击绿色盒子在点击文字，以便大家能更好的分辨出这三个属性的使用区别
flutter: 绿色盒子被点击了
flutter: 文字点击事件回调

当属性设置为HitTestBehavior.opaque控制台输出结果
flutter: 文字点击事件回调
flutter: 文字点击事件回调

当属性设置为HitTestBehavior.translucent控制台输出结果
flutter: 文字点击事件回调
flutter: 绿色盒子被点击了
flutter: 文字点击事件回调
总结

Listener是Flutter中比较重要的功能性组件，它主要的功能是用来监听屏幕触摸事件，
事件回调可以获取对应的属性来个性化定制app功能。
```
